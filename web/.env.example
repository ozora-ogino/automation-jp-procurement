# Frontend Environment Variables
# Copy this file to .env and update the values

# API Base URL
# For development, this proxies through the dev server (see package.json proxy setting)
# For production, nginx proxies /api/* requests to the backend
REACT_APP_API_BASE_URL=/api/v1